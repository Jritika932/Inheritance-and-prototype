// Write a JavaScript function to retrieve all the names of object's own and inherited properties.

let object={ name:"ritika joshi", rollno:"14567", address: "zolo inxes blr"
}
let result=Object.keys(object)
console.log(result)